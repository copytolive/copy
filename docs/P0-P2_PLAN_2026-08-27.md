# P0-P2 Hyperliquid parity

Source checkpoint: `c47c985bfd4b16670a5905b6a8085a8e214e439c` (`checkpoint/history-v31-stable-20260827`).

## P0 — fee + exact OID allocation
- Preserve raw `closedPnl`, `fee`, `feeToken`, OID and fill class from Hyperliquid.
- Trading PNL uses `closedPnl - fee` for USDC-denominated fees.
- TP/SL child fill OIDs are allocated to their exact parent entry OID before any position/FIFO fallback.
- No balancing delta may be forced into LANGSUNG ENTRY merely to make an invariant pass.

## P1 — history coverage
- Fast path remains `userFills`/last-good cache.
- If the recent endpoint reaches its 2,000-fill ceiling, hydrate `userFillsByTime` with timestamp pagination, capped at Hyperliquid's documented 10,000 recent fills.
- Persist compact fill history and TP/SL parent-child classification by account.
- `historicalOrders` is limited to 2,000 recent orders; old entries outside its time coverage are marked classification-unknown instead of silently treated as direct.

## P2 — funding + metric semantics
- Hydrate `userFunding` separately.
- UI `PNL` remains Trading PNL after fees. Funding is not silently mixed into trade PNL.
- State exposes `accountPnl = tradingPnl + fundingPnl` for reconciliation.
- `RISK/REWARD` is explicitly realized average winner / average absolute loser, not planned TP/SL ratio.

## Release gates
- Exact child-OID attribution fixture.
- Fee-aware PNL fixture.
- >2,000 fill pagination fixture.
- Funding separation fixture.
- ALL/PENDING/DIRECT temporal browser test with rogue summary writers.
- Production loader verification and GitHub Pages browser smoke.
