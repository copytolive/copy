# P0-P2 Hyperliquid parity plan

Checkpoint source: `checkpoint/history-v31-stable-20260827` at `c47c985bfd4b16670a5905b6a8085a8e214e439c`.

P0: preserve fee/closedPnl fields, compute fee-aware trading PNL, and allocate TP/SL child fill OIDs to exact parent entry OIDs before any fallback matching.

P1: hydrate up to the 10,000 most recent fills via `userFillsByTime` pagination, keep fast first paint from recent/cache, and expose coverage metadata. `historicalOrders` remains limited to 2,000 recent orders; classification coverage must be explicit rather than silently treating unknown old entries as direct.

P2: hydrate `userFunding`, keep Trading PNL and Account PNL semantics distinct, and label realized average-win/average-loss as `REALIZED R/R` internally while preserving the existing compact UI label. Add browser/temporal acceptance and production verification.
